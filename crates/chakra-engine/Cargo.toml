[package]
name = "chakra-engine"
description = "In-memory workspace engine: symbol graph, atomic revisions, query service"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
chakra-domain = { path = "../chakra-domain", version = "0.1.0" }
arc-swap.workspace = true
thiserror.workspace = true

[lints]
workspace = true
