# v0.4.0 release-candidate readiness

Release-shell review on 2026-09-19: the Unix workflow previously sourced
`.zshrc` explicitly, which did not prove automatic startup-file loading.
It now launches `zsh -ic` with a clean environment. The exact workflow smoke
script passed against the existing macOS ARM64 candidate archive, including
checksum verification, version discovery through PATH and repeat-install
idempotence. Evidence: `target/kmp-fix/release-shell-review/smoke.json`.
The four bundle tests and the preflight identity test also passed. This
checks the workflow correction locally; the archive is not final immutable
candidate evidence, and other native targets remain pending.

Windows workflow review on 2026-09-19: the native installer smoke now
requires two successful installs with exactly one persisted user-PATH entry,
then rejects a deliberately incorrect archive checksum and checks that the
installed binary hash, runnable version and user PATH remain unchanged.
The exact original user PATH is restored in `finally`, including when an
assertion fails. Previously the smoke covered only one successful install
and restored PATH only on success. YAML parsing, the four bundle tests and
ten preflight identity scenarios passed locally after this change. PowerShell
and a Windows execution host are unavailable locally: these new assertions
have not executed, and fresh-process PATH discovery remains a separate
pending Windows requirement.

Kotlin follow-up on 2026-09-19 fixed omitted typed compiler options and a
Gradle metadata-invalidation regression caused by the earlier script filter.
Four real Gradle model contracts passed. The real KMP scenario then passed
in 242.08 seconds, including source edits and a build edit adding a source
root (six callers after reimport). See the [Kotlin report](v0.4.0-kotlin-acceptance.md).
This does not complete the remaining native/client/frozen-candidate gates.

Shared LSP review on 2026-09-19 fixed notification-drain starvation in
local commit `59bf735` (`fix: bound LSP event draining to queued batches`).
[Review and validation](v0.4.0-lsp-drain-review.md): full worktree suite
917 passed/31 ignored; final LSP tests 14 passed; Clippy, formatting and
dependency policy passed. Existing partial staging was preserved exactly.
The commit has not been pushed, and the remaining candidate changes have
not yet passed the complete final patch review.

Reviewed CLI fixes were recorded locally in `231d35a` on 2026-09-19
(`fix: preserve agent setup and doctor output contracts`). The pre-commit
CLI rerun passed 82 unit tests and one executable JSON regression; complete
worktree validation remains 919 passed/31 ignored. Unrelated index entries
were verified unchanged. This commit has not been pushed.

Kotlin integration was committed locally on 2026-09-19 as `0b85db1`
(`fix: validate Kotlin imports and precise enrichment`). Exact-patch self-review
and architecture review found no remaining commit blockers. The fresh
pre-commit worktree run passed 919 tests with 31 ignored; formatting, workspace
Clippy, dependency policy, support-matrix consistency and whitespace checks
passed. The commit preserves unrelated staged changes. It reuses workspace
`tempfile`/Unix `nix` and includes the reviewed `rustls` lock update to 0.23.45.
The validation covers the current worktree, including other unfinished changes;
it is not isolated-commit or frozen-candidate evidence. The full real-provider
matrix must still run on the final candidate. Local evidence:
`target/kmp-fix/kotlin-final-commit/commit.json` and `review.json`.

The Docker provider gate was committed locally on 2026-09-19 as `ea95063`
(`test: complete Docker language-provider acceptance gate`). It selects all
ten real-provider adapters plus Kotlin Gradle import contracts, aggregates
failures without skipping later providers, and pins Maven/Gradle/Android
prerequisites with verified downloads. Five wrapper tests passed, including
retaining a failed workspace status after successful provider stages; Bash
syntax and whitespace checks passed. The Dockerfile, wrapper and four new
provider-test files match the prior successful Docker matrix hashes. No Rust
source changed after the latest 919-test worktree validation. This commit
records the tested gate; it does not claim a new frozen-candidate Docker run.
Evidence: `target/kmp-fix/provider-gate-final-review/`.

Two additional local commits on 2026-09-19 record reviewed acceptance work:
`8befd63` verifies committed-source identity and worktree-overlay isolation
across cached HEAD transitions; `9a7a227` adds checksummed installer bundles,
release-branch preflight, and the mandatory Docker language-acceptance job.
The latter passed 12 Unix installer fixture tests, four bundle tests and ten
manual-preflight identity scenarios. Ruby parsed both workflow YAML files and
all referenced jobs exist; native Actions execution remains pending. Neither
commit was pushed. Evidence: `target/kmp-fix/registry-final-review/` and
`target/kmp-fix/release-gate-final-review/`.

Core documentation was committed locally as `4a22ee3` on 2026-09-19.
README, changelog, contributor workflow, configuration example and client
matrix now distinguish preparation, implemented behavior, measured evidence
and remaining gates. A read-only GitHub check still returned `v0.3.2` as the
latest non-draft stable release. Independent consistency validation of all
20 paired-run manifests reproduced the tool/token counts and aggregate
medians; it does not replace source scoring or repeat model sessions.

Status on 2026-09-19: **not ready**. This is a working evidence register,
not permission to publish. The active objective includes publication once
ready. The user also required local-only impact bookkeeping and no GitHub
changes for now; clarification about a non-publishing remote preflight is
still pending. No push, tag, release merge or publication has been performed.

Scope authority: [release acceptance #195](https://github.com/crystaldaking/crystal-chakra/issues/195),
the accepted v0.4.0 milestone, SPEC, and relevant ADRs. The issue body was
read on 2026-09-18. Existing issues are references; this task creates none.

| Requirement | Current evidence | Remaining verification |
| --- | --- | --- |
| Kotlin JVM, Android, Multiplatform | The latest full Docker wrapper passed all five Kotlin scenarios: Maven, Gradle JVM, Android, mixed Kotlin/Java and Multiplatform, plus five real Gradle import contracts, including compiler-plugin and multi-module coverage. [Kotlin report](v0.4.0-kotlin-acceptance.md). | Semantic binding/import code review completed on 2026-09-19 after fixing deferred-call ownership; 919 workspace tests passed. Native Windows path regression, full final candidate review and frozen-candidate checks remain. Gradle cancellation probes passed, including a task that catches interruption. |
| All supported languages | The full Docker wrapper passed 909 workspace tests, 15 explicit real-provider scenarios across ten adapters and five real Gradle import contracts. [Reliability report](v0.4.0-provider-reliability.md). | Run frozen-candidate gates. Later additions include two update-handler tests, a standalone-script regression and the Gradle DSL path-routing fix; their focused/current-workspace validation is recorded separately. |
| Controlled paired evaluation and scope decision | All ten Codex pairs completed; both conditions scored 0.95, with 5.56% median ordinary-call reduction and -25.49% median time reduction in the registered population. The user explicitly accepted the [local impact no-go](v0.4.0-paired/ANALYSIS.md) on 2026-09-18. Conditional #191–#194 work is excluded from local 0.4.0 scope; other accepted features remain mandatory. | Remote #190–#195 issue/milestone bookkeeping is deferred by the user's explicit instruction to leave GitHub unchanged. This is distinct from the completed local scope decision. |
| Four agent clients | Codex CLI 0.154.0 passed real fresh-session adoption and unavailable-server fallback. [Compatibility matrix](../support/agent-clients.md) links sanitized evidence. | OpenCode is blocked by a client crash and OAuth 401; reauthentication is unavailable to the user for now. Claude Code and Cursor still need real-session execution. |
| Installers and update checks | All 12 Unix installer fixture tests passed on the macOS host on 2026-09-18 (`target/kmp-fix/installer-host.log`); release workflow defines native builds/smokes. | Verify all four native targets with real candidate binaries, repeat install, fresh-shell PATH, version, update discovery, failure preservation, checksums, and provenance. A real local macOS ARM64 release binary also passed packaging, installation, fresh interactive zsh PATH, repeat install and checksum-failure preservation. The actual Linux x86-64 binary passed packaging, install, fresh Bash, repeat install, failure preservation and public update discovery in an amd64 container on this ARM64 Mac. Physical/native release-runner checks, Windows/Intel execution, controlled newer-release discovery and final immutable candidate provenance remain pending. |
| Shared configuration, diagnostics, local reports | The integrated four-client setup/doctor/report/remove workflow passed, including JSON integrity, sanitized output, overwrite protection and preservation of user instructions. The [CLI patch review](v0.4.0-cli-patch-review.md) completed on 2026-09-19 without new blocking findings; corresponding regressions pass in the latest 919-test workspace run. | Repeat final immutable-candidate checks after freeze. |
| Provider crash/restart reliability | [Current-source validation](v0.4.0-provider-reliability.md): 100 crash/restart repetitions across ten providers passed in two parallel lanes while the full workspace suite ran; that suite also passed (912 tests, 30 ignored). The full Docker real-provider matrix subsequently passed. Existing cancellation/fallback/shutdown assertions remain intact. | Final immutable-candidate validation remains required. |
| Candidate review and quality | Focused Kotlin/worker checks and preliminary full Rust gates pass; complete worktree still contains unfinished changes. Manual preflight now accepts the matching release branch before main merge; all 10 branch/tag identity scenarios in `tools/test_release_preflight.py` pass. | Self-review, architecture review, full repository validation, exact staged-patch review, and actual release preflight on the final candidate. The identity test does not prove native builds or attestations. |
| Versions, support claims, documentation | Packages identify 0.4.0; Kotlin remains unadvertised. | Regenerate support artifacts from actual passing evidence; reconcile release notes/docs and candidate branch. |

The publication-only criteria of #195 (annotated tag on reviewed main,
public GitHub assets/latest release, public-download verification, merge-back,
milestone closure) remain incomplete. They are part of the eventual release
objective, subject to release readiness and the current restriction on GitHub
changes. A non-published candidate cannot claim that public artifacts already
exist or passed checks.

## Chronological supporting evidence

The entries below describe intermediate work on 2026-09-18 and later
follow-ups. Earlier failures and pending steps are retained for traceability;
the current requirement table above takes precedence for status.

The Kotlin acceptance criteria in #209 were re-read on 2026-09-18. They
explicitly include mixed Kotlin/Java boundary behavior and revision-bound
synchronization. The initial real regression omitted `JavaPeer.javaCaller()` before reaching
the Java-only edit. Raw protocol probes showed that Java references are
indexed but omitted by the hierarchy route. The adapter now synchronizes
Java companion documents while retaining Kotlin-only query ownership; its
semantic route passed the real mixed-JVM test, including Java-only edit
freshness and Kotlin-to-Java calls. Expanded overload/method-value cases also passed in 103.51 seconds;
final candidate checks remain pending. See the Kotlin acceptance report.
The temporary incomplete-coverage marker for
mixed KMP/non-KMP modules fixes misleading completeness claims; it does not
implement the omitted modules.

Additional local installer evidence on 2026-09-18: the Linux/amd64 container
completed the Unix fixture suite, with eleven scenarios executed and the
fresh-zsh-shell scenario skipped because zsh is absent
(`target/kmp-fix/installer-linux.log`). The first run exposed an incorrect
aggregate count: the skipped case was counted as passed. The runner now
reports skips separately; repeat runs confirm 11 passed/1 skipped on Linux
and 12 passed/0 skipped on macOS. These fixtures use test executables, not
packaged 0.4.0 release binaries.
A read-only check of the latest five GitHub release-workflow runs found
successful 0.3.x runs only; they do not validate this candidate.

Passing a prepared-model diagnostic does not satisfy the default Kotlin gate.
Likewise, a handshake is not agent adoption, configured CI is not a completed
native run, and a written evaluation protocol is not measured paired results.

Current Kotlin/Java validation follow-up: the expanded real-server regression
passed on 2026-09-18 in 103.51 seconds. The focused host suite passed 71 tests
with seven opt-in tests ignored (real-server and Gradle contracts are run
separately). Full-workspace Clippy and formatting pass on this tree; full
workspace tests (909 passed, 27 ignored) and dependency policy checks
also passed, with results in
`target/kmp-fix/java-final-validation/`. This directory name is an evidence
location, not a declaration of release readiness.

Native macOS ARM64 follow-up: `cargo build --locked --release --package
chakra-cli --bin chakra` passed, and `target/kmp-fix/native-macos-smoke.py`
verified the actual binary through archive creation, clean install into an
isolated HOME, automatic startup-file loading by a fresh interactive zsh,
repeat-install PATH idempotence, and checksum-failure preservation. The
archive and binary hashes are recorded in
`target/kmp-fix/native-macos/result.json`; the archive and SHA256SUMS are
retained beside it. This candidate includes uncommitted changes, so it is
not final immutable release provenance and must be rebuilt after freeze.

The same native binary's `chakra update --check` reached the public GitHub
API, reported latest stable v0.3.2 and correctly returned exit 0 for installed
0.4.0 (`target/kmp-fix/native-macos/update-check.json`). This proves current
release discovery, not the newer-release branch. A dogfooding session using
the rebuilt release binary also returned successful `status`, `repo_map` and
`symbol_search` responses, including the new `supports_query_language`
method with syntax provenance from the worktree overlay
(`target/kmp-fix/dogfood-current-release.json`). Providers were explicitly
disabled in that navigation session; it is not real-agent adoption evidence.

Integrated CLI follow-up found and fixed an output-integrity defect:
`chakra doctor --json` appended human-readable diagnostics after the JSON
document, and `--json --report` also appended report notices. Exit-status
calculation is now independent of printing; report notices use stderr.
The executable-level regression `crates/chakra-cli/tests/doctor_json.rs`
failed before the fix and passed afterward, covering plain JSON, report
creation, overwrite rejection/force and invalid project configuration.
All 79 CLI tests passed (`target/kmp-fix/doctor-json-after.log`). This is
a follow-up after the 909-test host validation; the concurrently running
Docker matrix compiled its CLI before this change.

The repeated integrated setup/report test then exposed another remaining
doctor defect: a present remote `mcpServers.chakra` entry with a `url` but
no `command` is reported as absent, with exit 0, rather than as incompatible
with the local Chakra registration. Reproduction: replace the generated
Claude `.mcp.json` entry with a URL entry and run `doctor --agent claude`.
`target/kmp-fix/setup-report/steps.json` records the observed warning. The
end-to-end setup/report gate remains incomplete until registration parsing
distinguishes malformed/remote entries from genuinely absent entries.
No GitHub issue was created, per the task constraint.

The registration defect is now fixed with explicit absent/disabled/local
states and validation errors for remote or malformed entries. Arguments
with non-string elements are rejected rather than silently filtered. The
regression covers Codex TOML and all three JSON clients; disabled entries
remain warnings. All 80 CLI tests and CLI Clippy passed
(`doctor-registration-after.log`, `doctor-registration-clippy.log`).
After rebuilding the release binary, the integrated setup/doctor/report/remove
scenario passed for all four configurations, including dry-run, idempotence,
JSON parsing, bounded sanitized report, overwrite protection, a remote-entry
secret canary, and preservation of existing instruction text (the managed
block separator remains). Evidence: `target/kmp-fix/setup-report/result.json`.
No agent sessions were launched, so client adoption remains unproven.

Instruction ownership follow-up: review against ADR-0055 found that removal
normalized triple blank lines throughout the user's instruction file and
accepted ambiguous marker sequences. The new regression failed before the
fix (`target/kmp-fix/setup-removal-before.log`). Removal now concatenates
only the exact prefix and suffix outside the managed block; setup and removal
reject duplicate/nested/dangling extra markers. All 81 CLI tests and CLI
Clippy passed (`setup-removal-after.log`, `setup-removal-clippy.log`). The
release binary was rebuilt and the integrated four-client configuration
scenario passed again, preserving the complete outside-block bytes.

Local acceptance-surface inventory (2026-09-18) confirms macOS ARM64 and
Docker Linux/amd64. Codex and OpenCode commands exist on PATH; Claude Code,
Cursor agent and PowerShell commands do not. No Cursor/Claude or
Parallels/VMware/UTM application was found in the two standard Applications
directories. Authentication was not inspected and command presence is not
client-session evidence. Windows/macOS Intel and the missing client surfaces
therefore still need external execution or user-provided access. The prior
availability question remains unanswered. Machine-path-free inventory:
`target/kmp-fix/local-acceptance-surfaces.json`.

The full Docker wrapper after the Kotlin/Java fix completed with exit 0
(`target/kmp-fix/all-languages-java-fix.log`): 906 workspace tests passed
(27 opt-in tests ignored in that stage), followed by 15 explicit real-provider
scenarios across ten adapters and two real Gradle model contracts. All five
Kotlin scenarios passed: Maven, Gradle JVM, Android, mixed Kotlin/Java and
Multiplatform. The workspace CLI was compiled before the later doctor and
instruction-removal fixes; those have separate current CLI validation.

The user explicitly authorized separate Codex/OpenCode test sessions. Initial
adoption probes now exist under `target/kmp-fix/client-adoption/`; they are
not paired-corpus runs. Codex 0.154.0 read the generated instructions but
initially lacked the project MCP server; a temporary trusted named profile
exposes the registration in `codex mcp list`, and session loading is still
being diagnosed. OpenCode 1.18.30 fails with a `SystemPrompt.environment`
TypeError even when Chakra is disabled; one run also records OpenAI OAuth
refresh failure (401). These attempts do not satisfy successful adoption.

Codex adoption follow-up now passes for CLI 0.154.0 on macOS ARM64 with a
trusted named profile and ordinary configuration loading. Fresh positive and
controlled missing-executable sessions both answered the normal navigation
task correctly; the positive session made four Chakra tool calls without a
Chakra-specific prompt, and the negative session disclosed fallback.
Sanitized raw events, answers, fixture contents and candidate hash are
committed-ready under `docs/evaluation/v0.4.0-clients/`; see the
[client matrix](../support/agent-clients.md). Source contents were checked
unchanged. This is not paired-corpus evidence. OpenCode's minimal no-Chakra,
no-tools control still fails with the same client TypeError.

OpenCode authentication follow-up: the user cannot reauthenticate at present.
Do not retry the same OAuth refresh or count this client as verified. The
independent TypeError remains reproducible without Chakra. Authorized paired
Codex evaluation proceeds independently; all five pinned repositories and
expected task paths have been checked locally. The seeded five/five order and
measurement rules were recorded before the first session in
`target/kmp-fix/paired/plan.json`. At that stage no scope decision had been
made; the user subsequently accepted the local no-go recorded in the table.

Paired evaluation completed locally on 2026-09-18: twenty real Codex sessions,
ten reviewed pairs, and four successful doc-patch replays on post-mutation
source. Both conditions had nine successes and one partial outcome. All
run manifests and referenced artifacts are retained under
`docs/evaluation/v0.4.0-paired/runs/`; the analysis explicitly distinguishes
measured gates, an unnamed residual case in the general expansion rule, and
the standalone impact no-go. No new public query is justified by this study.
Scope bookkeeping and the other release gates remain incomplete.

A read-only Intel execution probe on the current Mac failed with
`Bad CPU type in executable` (`target/kmp-fix/macos-intel-runtime-check.json`).
The task did not install or configure a translation runtime. This does not
supply the missing macOS Intel native validation.

Distribution follow-up on 2026-09-18: [the Linux package check](v0.4.0-distribution/README.md)
passed with the actual optimized binary, not a fixture executable. The Docker
image is Debian bookworm/amd64, executed on macOS ARM64; it does not establish
Ubuntu 22.04 ABI compatibility or replace final native-runner evidence. The
binary, archive and installer hashes are recorded. All 267 candidate source
hashes still match the paired evaluation; only distribution workflow/docs
changed in this follow-up.

Review also found that installer scripts were omitted from published assets,
checksums and provenance. The workflow now assembles and attests both scripts
with the four archives; installer-smoke downloads this complete bundle,
verifies its manifest, and executes the bundled scripts. Publication is still
conditional on a tag push and all acceptance jobs; it was not executed.
Four actual assembly/checksum regression tests and all ten preflight identity
cases pass, as do twelve Unix installer fixtures and YAML/dependency-graph
checks. PowerShell execution and actual GitHub attestations remain unverified.

Kotlin compiler-argument follow-up now preserves validated `freeCompilerArgs`
through the build's Kotlin parser. Three real Gradle contracts pass, including
unknown/conflicting settings and untracked `@argfile` rejection. The KMP
real-server scenario with flags passed before the final argfile guard;
see the [Kotlin report](v0.4.0-kotlin-acceptance.md) for exact boundaries.
The new full workspace run is **failed**, not final-ready: the HEAD-transition
registry test expected `ColdBuild` but observed `MemoryReuse`. Investigation
and a passing repository gate remain outstanding. This production change
also means the earlier paired/distribution source hashes describe their
recorded candidate, not the current complete source tree.

The HEAD-transition mismatch is now resolved at the test-contract level;
[the review](v0.4.0-head-transition-review.md) explains the allowed cache-hit
path after a discarded publication attempt. Production cache/publication code
is unchanged. The corrected test adds explicit commit-content and dirty-overlay
isolation checks; the direct single-builder cache assertions remain intact.
All seven registry tests and 24 concurrent repetitions passed. The current
host gates now pass: 912 workspace tests, 28 ignored external/opt-in tests,
full Clippy, formatting, dependency policy and diff checks. This supersedes
the failed workspace-gate status above, not the other release blockers.

The current-source KMP real-server run also passed, including the final
argfile guard and build/source-root reimport (204.22 seconds;
`target/kmp-fix/head-review/kmp-current.log`). All six expected callers were
present after reimport. The other provider/project-shape and immutable final
candidate checks remain separately tracked.

Same-build KMP project dependencies now import through KGP's declared artifact
producers. Four real Gradle contracts pass, including explicit rejection of
included-build dependencies and invalidation of a prior imported root. The
expanded two-module KMP real-server scenario passes in 186.19 seconds with
cross-module calls, JVM/JS/Native and source/build reimport checks. See the
[Kotlin acceptance report](v0.4.0-kotlin-acceptance.md) for failed intermediate
fixtures and exact evidence boundaries. Host validation passed with 912 tests
and 29 ignored; the final fixture-only edit was then verified by the real LSP.
These results do not clear composite-build, compiler-plugin, native-platform,
client-adoption or final-candidate gates. OpenCode reauthentication remains
unavailable per the user; no retry or successful-client claim was made.

Compiler-plugin follow-up: the KMP exporter now preserves KGP plugin paths
and options, including Serialization and All-open, rather than silently
omitting them. The corrected source-set API handles Native as well as JVM/JS;
conflicting platform-only plugin artifacts reject import and invalidate the
old model. All five real Gradle contracts pass, and the two-module real-LSP
KMP fixture with both plugins passes in 217.56 seconds, including six callers
after Gradle/source-root reimport. Full host gates pass with 912 tests and
30 ignored. The [Kotlin report](v0.4.0-kotlin-acceptance.md) records the
before/after regression, intermediate Native failure and current results.
This clears the demonstrated plugin-settings omission; arbitrary generated
declarations, remaining typed compiler options and the other release gates
remain unverified. No commit, release branch or publication was created.

Support-artifact follow-up: `python3 tools/check_support_matrix.py --check`
passed for all 12 language manifests; Kotlin remains unadvertised pending
its outstanding precise gates. `cargo run --locked -p chakra-conformance --
corpus --verify` found zero problems in the recorded corpus evidence. Fresh
conformance emission for all 12 languages matched `docs/support/conformance`
exactly (`diff -r`, exit 0). Commands and output are under
`target/kmp-fix/lifecycle-review/`. Corpus verification validates the saved
evidence, not a new public-repository indexing run. These checks do not
replace the real-provider matrix or native-platform validation.

Standalone script and Gradle Kotlin DSL boundaries are now explicitly tested.
Standalone import failure degrades honestly; Gradle DSL paths are excluded
from precise queries while retaining syntax and metadata tracking. The new
routing regression failed before the fix and passes afterward; the expanded
real Gradle JVM scenario passed in 161.25 seconds. Current host gates pass
with 915 tests and 31 ignored, plus full Clippy, formatting, dependency policy
and diff checks. See the Kotlin acceptance report for protocol and
regression evidence. Final immutable-candidate and native/client gates remain.
